<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/quiz.css">
    <title>Quiz Game</title>
</head>
<body>
    <div class="header">
        <div class="scoreboard">
            Score: <span class="score">0</span>/5
        </div>
        <div class="question-count">
            Question: <span class="current-question">1</span>/5
        </div>
    </div>
    
    <div class="question-window">
        <%= currentQuestionText %> <!-- Display the current question text here -->
    </div>
    
    <!-- Display answer options -->
    <div class="option-container">
        <div class="option" data-index="0">
            <%= currentOptions[0] %> <!-- Option 1 -->
        </div>
        <div class="option" data-index="1">
            <%= currentOptions[1] %> <!-- Option 2 -->
        </div>
        <div class="option" data-index="2">
            <%= currentOptions[2] %> <!-- Option 3 -->
        </div>
        <div class="option" data-index="3">
            <%= currentOptions[3] %> <!-- Option 4 -->
        </div>
    </div>
    
    <div class="feedback">
        Your feedback will appear here
    </div>
    
    <div class="progress">
        <!-- Dynamically generate circles based on questions -->
        <% for(let i = 0; i < 5; i++) { %>
            <div class="circle <% if(questionStates[i] == 'correct') { %>correct<% } else if(questionStates[i] == 'incorrect') { %>incorrect<% } else { %>unanswered<% } %>"></div>
        <% } %>        
    </div>
</body>
</html>
